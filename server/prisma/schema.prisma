generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://jasja:admin@localhost:5432/tagdate-test?schema=public"
}

model User {
  id             Int      @id @default(autoincrement())
  name           String
  // bio            String?
  age            Int
  // interestedIn   String
  // location       String
  // profilePicture String?
  // activity       String
  // matches        String[]

}
